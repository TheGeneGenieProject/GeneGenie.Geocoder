{
    "App": {
        "GeocoderSettings": [
            {
                "ApiKey": "To avoid putting keys in source, override in appsettings.Development.json or environment vars, never here.",
                "GeocoderName": "Google"
            },
            {
                "ApiKey": "To avoid putting keys in source, override in appsettings.Development.json or environment vars, never here.",
                "GeocoderName": "Bing"
            }
        ]
    },
    "Logging": {
        "IncludeScopes": true,
        "LogLevel": {
            "Default": "Debug"
        }
    }
}